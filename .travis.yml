language: python
python:
  - 2.7
  - 3.3
  - 3.4
install:
  - pip install -q err pytest --use-wheel
  - pip install -q coverage coveralls --use-wheel
script:
  - coverage run --source factoid -m py.test
after_success:
  - coveralls
notifications:
  email: false
